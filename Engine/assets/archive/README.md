# Archived Asset References

These folders are kept only as reference material.

- `legacy_menu_reference/` preserves the older menu-atlas source set.
- `unused_reference_images/` preserves images that are not part of the active
  example runtime.

Nothing in this archive folder should be treated as the current source of truth
for menu art, atlas generation, or packaged runtime assets.

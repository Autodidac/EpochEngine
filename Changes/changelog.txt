# Epoch Changelog

## [v0.83.0]
- Shipped the first dedicated `Release|x64` updater-shell runtime as the downloadable `main.zip` payload, so first launch now lands directly on the minimal bootstrap UI instead of the full launcher surface.
- Replaced the source-updater dependency bootstrap with a managed toolchain flow that can download `vcpkg`, provision Git when needed, patch the `glad` port for current CMake, and rebuild from source without assuming a preinstalled development environment.
- Moved live source-update work into shorter managed work roots and kept the console/process output readable, which makes the shell demonstration path much easier to follow while leaving the rebuilt runtime path aimed at the full engine.

## [v0.82.60]
- Shipped the updater shell as a dedicated software-only bootstrap runtime with a single large update action and a minimal packaged payload.
- Changed updater-shell updates to close the shell window before the console-driven update work begins, preventing the shell from hanging inside the render loop during package or source updates.
- Verified the live packaged shell can fall through from release `0.82.60` to newer source builds on `main`, rebuild the runtime, replace the binaries, and relaunch successfully.

## [v0.82.51]
- Rolled the active repo surfaces forward after the packaged `0.82.50` updater release so `main` stays ahead of the downloadable runtime again.

## [v0.82.50]
- Shipped the Windows source updater handoff fix so the detached worker resolves the live executable path correctly, replaces the rebuilt runtime from `<source-root>/x64/Debug`, and relaunches the updated app cleanly.
- Moved native `vcpkg` and `MSBuild` output into updater log files so restore/build activity no longer dumps raw junk into the live console.
- Switched the sample project include/library paths to repo-relative vcpkg locations so source snapshot builds no longer depend on hardcoded local directories.

## [v0.82.49]
- Rolled the active repo surfaces forward after the packaged `0.82.48` detached-worker updater release so `main` stays ahead of the downloadable runtime again.

## [v0.82.48]
- Moved Windows source updates into a detached worker process so download, restore, build, replacement, and relaunch can continue after the live runtime exits.
- Kept source updates pointed at the direct `main.zip` repository snapshot, added a visible-by-default worker path, and allowed silent worker launches with `EPOCH_UPDATER_SILENT=1`.
- Raised the worker-side build tolerance to three MSBuild attempts after restore and extended the runtime unlock wait so the final replacement step is less timing-sensitive.

## [v0.82.47]
- Rolled the active repo surfaces forward after the packaged `0.82.46` updater repair release so `main` stays ahead of the downloadable runtime again.

## [v0.82.46]
- Kept the source updater on the direct `main.zip` repository snapshot instead of GitHub release assets.
- Adjusted the source rebuild path so it restores manifest dependencies first, then retries the compile pass once before giving up.
- Kept `epoch_source_update.log` and `epoch_update_handoff.log` as the runtime-side diagnostics for rebuild and replacement failures.

## [v0.82.45]
- Rolled the active repo surfaces forward after the packaged `0.82.44` updater repair release so `main` stays ahead of the downloadable runtime again.

## [v0.82.44]
- Replaced the Windows updater `system()/cmd/start` path with hidden process launches for `vcpkg`, `MSBuild`, and the final handoff batches.
- Added `epoch_source_update.log` alongside `epoch_update_handoff.log` so source rebuild failures and post-build replacement failures leave concrete runtime-side diagnostics.
- Fixed the updater source-version path to point at `Engine/modules/aengine.version.ixx` instead of the stale `version.txt` location.

## [v0.82.43]
- Rolled the active repo surfaces forward after the packaged `0.82.42` release so `main` stays ahead of the downloadable runtime again.

## [v0.82.42]
- Repaired the Windows updater handoff batch again by switching to proper batch variable quoting and explicit executable replacement.
- Added `epoch_update_handoff.log` in the runtime folder so failed packaged/source handoffs leave behind a concrete trail instead of silently stalling.
- Refreshed the active release surfaces for the repaired `0.82.42` runtime drop.

## [v0.82.41]
- Repaired the Windows updater handoff so packaged and source updates explicitly replace the runtime executable instead of silently leaving the old binary in place.
- Kept the updater logging path sanitized so updater status stays readable in the commandline pane while the handoff runs.
- Refreshed the active release surfaces for the repaired `0.82.41` runtime drop.

## [v0.82.40]
- Routed updater status back through Epoch logging with sanitized updater messages so the commandline pane stops showing the raw `♪◙` newline artifacts.
- Kept the newer-source fallback intact so a confirmed update still rebuilds from source when no newer packaged runtime exists.
- Refreshed the active release surfaces for the repaired updater runtime drop.

## [v0.82.39]
- Rolled the active repo surfaces forward after the packaged `0.82.38` release so `main` stays ahead of the downloadable runtime again.

## [v0.82.38]
- Fixed updater fallback so a confirmed update now rebuilds from source when `main` is newer and no newer packaged runtime exists.
- Removed the duplicate source-version probe so source update checks stop printing the same source comparison twice.
- Simplified the updater status path to one direct console stream during source/package update work.

## [v0.82.37]
- Rolled the active repo surfaces forward after the packaged `0.82.36` release so `main` stays ahead of the downloadable runtime again.

## [v0.82.36]
- Restored a single visible updater status path by only emitting direct console output when the engine logger is not already configured to own console output.

## [v0.82.35]
- Rolled the active repo surfaces forward after the packaged `0.82.34` release so `main` stays ahead of the downloadable runtime again.

## [v0.82.34]
- Removed the updater module's extra direct console writer so update status now appears once through the engine logging path instead of duplicating in the commandline view.

## [v0.82.33]
- Rolled the active repo surfaces forward after the packaged `0.82.32` release so `main` stays ahead of the downloadable runtime again.

## [v0.82.32]
- Reworked updater version probes to download into unique temp files instead of a shared `remote_version.txt`, preventing file-in-use failures when packaged and source checks run back to back.
- Kept the source-update availability messaging separate from packaged-release detection so the updater can point at `main` cleanly without confusing the release path.
- Cleaned the editor update confirmation modal spacing and trimmed the README snapshot back to a concise current-state summary.

## [v0.82.31]
- Rolled the active version and docs forward to `v0.82.31` immediately after the `0.82.30` updater rebuild release so the live tree stays ahead of the packaged runtime again.

## [v0.82.30]
- Restored the source-update flow so it now downloads the latest `main` snapshot, restores manifest dependencies with `vcpkg`, rebuilds `ConsoleApplication1`, and replaces the running runtime from the rebuilt output instead of stopping after extraction.
- Fixed updater version comparison so builds newer than the latest packaged release stop trying to downgrade themselves.
- Normalized updater console line emission for redirected output so captured update logs stop leaking raw CR/LF control glyphs.

## [v0.82.29]
- Rolled the active version and docs forward to `v0.82.29` immediately after the `0.82.28` update-flow release so the live tree stays ahead of the packaged runtime again.

## [v0.82.28]
- Cleaned up the editor update confirmation flow so packaged updates and source downloads are separate explicit actions instead of one ambiguous destructive button.
- Added a source-snapshot command that downloads the latest `main` source tree beside the runtime without rebuilding or replacing the running binary.

## [v0.82.27]
- Rolled the active version and docs forward to `v0.82.27` immediately after the `0.82.26` release so the live tree stays ahead of the packaged updater build.

## [v0.82.26]
- Restored visible console feedback for GUI-triggered update checks so the updater prints what it is doing and explicitly reports when no newer packaged release is available.

## [v0.82.25]
- Rolled the active version and docs forward to `v0.82.25` immediately after the fixed `0.82.24` full release so the live tree stays ahead of the packaged updater snapshot.

## [v0.82.24]
- Fixed the updater to query the latest real GitHub release instead of reading `main`, so the reported version and the downloadable `main.zip` asset stay in sync.
- Added GitHub API headers to the download helper so updater version checks use the release API cleanly.

## [v0.82.23]
- Rolled the active version and docs forward to `v0.82.23` immediately after the fixed `0.82.22` release tag so the updater has a new target to detect.

## [v0.82.22]
- Fixed the updater to target the shipped `main.zip` release package instead of stale direct-executable URLs, and aligned the built-in version URL to `Engine/modules/aengine.version.ixx`.
- Updated the Windows install path so the updater expands the package in place and replaces the runtime directory, rather than pretending GitHub serves a single replacement `.exe`.
- Routed updater status lines through Epoch logging so version/download messages no longer leak raw control-character glyphs into captured command output.

## [v0.82.21]
- Rolled the active version and current docs forward to `v0.82.21` immediately after the `0.82.20` release tag so the updater has a newer target to detect.

## [v0.82.20]
- Rolled the active version, README, docs, and release-note surfaces forward to `v0.82.20` so the updater has a fresh live target to detect during manual testing.

## [v0.82.19]
- Fixed the self-update flow so it now targets the currently running executable instead of the old hardcoded `updater.exe` replacement path.
- Added explicit failure reporting for update handoff/install failures so the app no longer looks like it updated successfully when the binary download or replacement step did not complete.
- Refreshed the active version surfaces to `v0.82.19`.

## [v0.82.18]
- Fixed the remaining Windows script compiler spawn bug by using a direct executable launch for real LLVM paths instead of the PATH-search variant, which was still splitting `Program Files` incorrectly during editor-run script builds.
- Refreshed the active version surfaces to `v0.82.18`.

## [v0.82.17]
- Consolidated the older versioned release-note markdowns into a single archive file so the `Changes/` folder keeps one historical notes surface while new releases continue to get their own markdown.
- Removed the accidentally tracked Windows `.exp` script-build artifact from the repo and added ignore rules for `.exp` files and the local `Temp Script Test/` workspace.
- Refreshed the active version surfaces to `v0.82.17`.

## [v0.82.16]
- Fixed the Windows editor script compiler path so it launches `clang++` directly instead of routing through a fragile shell command string that broke on `Program Files` paths.
- Kept the script source itself ASCII-clean and re-verified the rebuilt debug app launch from `x64/Debug` after the compiler-path fix.

## [v0.82.15]
- Replaced the old filewatch-oriented editor scripting flow with an engine-owned `Run` path that compiles and executes scripts against a stable host API, and swapped the default editor script to `rotate_all_entities`.
- Added reusable shared preview cameras with `Editor` and `FPS` modes, keyboard movement, and right-mouse look so the multicontext editor scene is no longer stuck on a fixed preview angle.
- Hardened updater version parsing so remote checks can consume either plain semver text or the full `aengine.version.ixx` module without printing BOM/comment garbage, then refreshed the active README/docs/version surfaces to v0.82.15.

## [v0.82.14]
- Rewrote the active README opening and feature map so Epoch is described more accurately as a world-class, modules-first, AI-enabled engine instead of a minimal runtime summary.
- Expanded the public feature list to call out internalized engine bootstrap, entry-point flexibility, multi-context orchestration, cross-platform build/editor freedom, module-first architecture, task-graph scripting reload, telemetry, and updater plumbing.
- Added direct markdown links from the README to the active docs, build surfaces, scripts, and project files, then refreshed the active version metadata to v0.82.14.

## [v0.82.13]
- Aligned the active backend base clear color to the darker Vulkan-style palette so mixed-context launches feel more like one engine instead of six unrelated demos.
- Moved SFML ahead of Vulkan in the default parented dock order and fixed the SFML shared scene preview so its grid/3D view stays clipped to the scene viewport instead of bleeding through GUI space.
- Rebuilt `StaticLib1` and `ConsoleApplication1` in `Debug|x64`, launched the real binary from `x64/Debug`, and verified the live top row now orders Raylib, SDL, and SFML ahead of Vulkan.

## [v0.82.12]
- Split the old mixed editor/menu flow into a launcher-first workflow where projects and games are chosen from the launcher and the editor focuses on desktop-style file, edit, scene, command, and help menus.
- Added scene preview switching in the editor between `Editor` and `None`, and gated the in-app update action behind an explicit confirmation modal because it can replace binaries and restart the session.
- Rebuilt `ConsoleApplication1` in `Debug|x64`, launched the real binary from `x64/Debug`, and refreshed the active README/docs to match the new launcher/editor workflow.

## [v0.82.11]
- Fixed parent-host shutdown ownership so only panes still docked under `EpochParent` close with the host, while undocked SDL, Raylib, and SFML panes survive as independent top-level windows.
- Fixed the last-window shutdown path so the process now exits when the final surviving undocked pane closes, even when that backend window is owned by a render thread.
- Re-verified live undock/redock and parent-close behavior on SDL, Raylib, and SFML, then refreshed the active README and current docs for the current engine tree.

## [v0.82.10]
- Restored real manual dock/undock interaction without the temporary `Alt` requirement by using a dedicated pane drag strip instead of hijacking the whole content surface.
- Re-verified live left-drag undock/redock behavior across SDL, Raylib, SFML, OpenGL, and Software, and confirmed the parent host still closes the full session cleanly afterward.
- Refreshed the active README and current docs to advertise the v0.82.10 manual docking interaction repair.

## [v0.82.9]
- Restored live undock/redock behavior by preserving each pane's original dock parent across future drags instead of forgetting it once a pane becomes top-level.
- Changed the docking grid arranger to lay out only panes that are currently parented under the live `EpochParent`, so undocked windows stop stealing grid slots or snapping back unexpectedly.
- Re-verified SDL, Raylib, SFML, OpenGL, and Software with live undock/redock interaction runs, then confirmed the parent host still closes the whole session cleanly.

## [v0.82.8]
- Fixed the parent-window shutdown path so docked backend panes close in place instead of being undocked during host shutdown.
- Re-verified that the parented multi-context layout stays docked at runtime and the process still exits when the main `EpochParent` host closes.
- Refreshed the active README and current docs to advertise the v0.82.8 docking/shutdown hotfix without rewriting historical release files.

## [v0.82.7]
- Hardened the parent-window close path so all tracked docked contexts receive shutdown and the engine session exits cleanly when the main host closes.
- Restored backend confirmation logging behind configurable `EPOCH_ENABLE_*CONFIRMATION_LOGS` switches so startup, upload, and teardown confirmations can be enabled without reintroducing per-frame spam.
- Refreshed the active README and current docs to advertise the v0.82.7 shutdown and diagnostics pass while keeping historical release files intact.

## [v0.82.6]
- Added the shared editor preview grid to the SDL and Software backends so they present the same scene-view framing as the existing GPU preview paths.
- Stopped the parented Raylib backend from replacing the original WGL device context after initialization, which stabilizes mixed-context startup.
- Refreshed the active README and current docs to advertise the v0.82.6 backend parity/stability pass without rewriting historical release files.

## [v0.82.5]
- Removed the unconditional per-frame Vulkan runtime diagnostic trace from normal runs and left it behind an explicit debug macro.
- Throttled slow-frame renderer warnings so startup no longer floods `Renderer.log` while the backend grid spins up.
- Removed the hot-loop SFML "frame render path" logging that was bloating `SFML.log` during startup.
- Refreshed the README and docs so the active build surfaces now cover Visual Studio, MSBuild, CMake presets, VS Code, shell scripts, and binary-folder launch paths.

## [v0.82.4]
- Restored the Vulkan editor scene pass so the backend renders the grid preview instead of only clearing the scene viewport.
- Matched the Vulkan editor preview framing and palette more closely to the OpenGL editor view, including calmer scene/background behavior during mixed-backend runs.
- Hardened the SFML close path so GPU atlas cleanup happens while the context is still valid and mixed-context shutdown stays quiet.

## [v0.82.3]
- Aligned the Vulkan preview palette with the OpenGL editor palette so the active backends present the same core scene colors.
- Stabilized Vulkan GUI presentation by keeping the last valid GUI batch during producer gaps and matching GUI atlas sampling behavior more closely to OpenGL.
- Refreshed the active release surfaces and notes to advertise the v0.82.3 Vulkan preview update without rewriting historical release files.

## [v0.82.0]
- Rebranded the public documentation and release metadata from the previous public name to Epoch.
- Synchronized engine version helpers and the legacy compatibility version header to v0.82.0.
- Moved the standalone `legacy/` archive into `Engine/legacy/` and added a single in-engine legacy archive guide.
- Rewrote the README, docs index, build guides, configuration guide, engine analysis, context audit, and runtime notes to match the current repository layout and backend status.

## [v0.81.23]
- Bumped the engine version constants to v0.81.23 so runtime banners and tooling remain aligned.
- Refreshed the README snapshot, configuration guide, and engine analysis notes to advertise the v0.81.23 release tag.
- Added the v0.81.23 release notes to keep the published metadata in sync.
- Documented the C++23 module-first baseline so build tooling and packagers can align module scanning and compiler expectations.

## [v0.72.9]
- Bumped the engine version constants and legacy headers to v0.72.9 so runtime banners and tooling remain aligned.
- Refreshed the README snapshot, configuration guide, and engine analysis notes to advertise the v0.72.9 release tag.
- Added the v0.72.9 release notes to keep the published metadata in sync.

## [v0.72.8]
- Converted additional legacy headers into module partitions so atlas utilities (`aatlasmanager.hpp`, `aatlastexture.hpp`) now expose `import aatlas.manager;` / `import aatlas.texture;`, the CLI surface is mirrored via `import acommandline;`, ECS mirrors (`import aentity.*`) stay in sync, and umbrella aggregates mirror the legacy include surface.
- Synced version metadata and documentation to advertise v0.72.8 with the refreshed import surface.
- Confirmed updater/configuration guidance pulls version strings from the runtime helpers so launch manifests match v0.72.8.

## Older releases
- Historical release notes remain in `Changes/release_notes_v*.md`.
- The older pre-rebrand release history has been preserved, but the public docs now use Epoch branding consistently.

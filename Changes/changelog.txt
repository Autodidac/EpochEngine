# Epoch Changelog

## [v0.83.87]
- Restored the SDL redock path to the visible proxy-host shell instead of
  collapsing the backend child directly into the parent during the shared
  proxy-redock helper, which was also the relayout path being reused under the
  six-context parent and could destabilize SDL during detach/redock cycles.
- Closed the remaining honest Phase 2 checklist items by removing the editor's
  last built-in sample-script fallback from the runtime command path and
  sourcing the default project shell state from `Engine/src/aeditor.scene.cpp`
  instead of hardcoded sandbox strings in `Engine/src/aeditor.cpp`.
- Updated the README promoted-window proof row so the detached-window shot and a
  live six-context desktop capture now sit side by side, making the docked vs.
  promoted state easier to compare at a glance.
- Reopened `main` above the published `v0.83.86` bootstrap/runtime release
  pair so source can move forward without blurring shipped artifact identity.
- Backed out the risky hidden-window OpenGL startup experiment and replaced it
  with a safer first-present placeholder path: OpenGL windows stay visible, the
  Win32 child proc paints the shell's dark neutral background until the first
  real present lands, and software no longer gets dragged into that startup
  workaround.
- Added a startup-only mode to `Tools/windows_parented_multicontext_harness.ps1`
  so README and doc proofs can capture startup layout/render state without
  synthetic drag input interfering with live multicontext behavior.
- Refreshed the README proof set around a current `v0.83.87` Windows
  multicontext capture plus six full-window per-backend startup screenshots,
  while keeping the promoted-window undock proof and WSL/Linux proof in place.
- Marked the Phase 2 workspace-shell organization item complete in the roadmap:
  the live shell is now explicitly organized around `Project`, `Scripts`,
  `Systems`, `AI`, and `Output`.
- Added a post-update startup cooldown to relaunched runtimes so packaged/source
  update handoff can pause briefly before the full context stack comes back up,
  reducing the longstanding “updated app restarts straight into a fragile
  OpenGL/multicontext bring-up” crash seam on Windows.
- Fixed the editor top-menu dropdown sizing against the real titled-window
  content budget so the last menu item text no longer disappears when shared
  GUI content-origin math changes.
- Added a generic staged research-import path through
  `Tools/research_import.ps1` and `workspace/research/staged/`, then closed the
  remaining Phase 1 roadmap items that were still treating raw PDFs/HTML as
  direct planning truth.

## [v0.83.86]
- Switched the active packaged updater contract onto versioned runtime
  archives such as `epoch_win10_x64_v0.83.86.zip` and
  `epoch_linux_x64_v0.83.86.tar.gz`, with the updater extracting packaged
  version info from the archive names instead of standalone text files.
- Kept the updater flow binary-first while preparing the new two-drop release
  protocol: a tiny bootstrap updater-shell release can move onto the newest
  packaged runtime release first, then source only if packaged parity is
  already reached.
- Reopened `main` above the published `v0.83.85` release so the source tree,
  docs, and version metadata can move forward without pretending the live
  branch is still the shipped snapshot.
- Published the `v0.83.85` GitHub release with both asset-bearing runtime
  packages attached: Windows `Epoch-v0.83.85-windows-x64.zip` and Linux
  `Epoch-v0.83.85-linux-main.tar.gz`.
- Refreshed the README proof set while `main` stays on `v0.83.86`: the Windows
  promoted-window undock proof now uses the supplied live screenshot, and the
  WSL/Linux OpenGL proof now comes from the current asset-bearing `v0.83.86`
  build output.
- Rewrote the README around the current source/runtime state and pruned
  `Images/readme/` down to the three proof images still in use: Windows
  multicontext, Windows promoted-window undock, and WSL/Linux OpenGL.
- Corrected the Windows multicontext README proof to come from the DPI-aware
  harness startup capture at full parent resolution instead of the earlier
  cropped desktop grab.
- Patched the live `v0.83.85` GitHub release with updater-compatible alias
  assets: `version.txt`, `windows_version.txt`, `linux_version.txt`,
  `main.zip`, `windows_main.zip`, and `linux_main.tar.gz`.

## [v0.83.85]
- Re-stabilized the SDL3/SFML3 Win32 six-pane undock path around the working promoted-proxy contract: the visible proxy shell is the real detachable top-level host, the backend child stays nested inside it, and detached placement clears the parent bounds instead of freezing visually trapped inside the parent.
- Kept the harness on a supporting role instead of letting synthetic input rewrite runtime truth: the Win32 probe now records more top-level window state, but clean human six-pane validation and subsystem logs are treated as the deciding evidence when the harness diverges.
- Bumped the public version to `v0.83.85` and updated README/build/runtime/smoke/config/analysis docs so the repo stops advertising stale `v0.83.63` guidance.
- Refreshed the README proof set for `v0.83.85`: the Windows six-pane source image now comes from a current asset-bearing release run, the README also includes a dedicated promoted-window undock proof, and the Linux proof now comes from the WSL2 OpenGL capture path under the asset-bearing `Engine/Bin/Clang-Release/` output.
- Aligned the packaged-runtime story across Windows and Linux: the main packaged runtime is the default release path, updater-shell mode is explicit/bootstrap-only, and Linux packaged assets are expected to stay source/version aligned instead of shipping as updater-only artifacts.
- Fixed the Linux main-runtime build path under WSL2 by moving the legacy runtime bridge declaration out of module-owned linkage and into a shared header, then keeping the CMake policy floor high enough that the system `glm` package no longer blocks configuration outright.
- Kept the project-shell and AI roadmap surfaces honest: generated projects still expose explicit build/output/script proof, and the two-role EpochBot plus MCP/control model stays staged/reviewable instead of drifting into blind autonomy language.

## [v0.83.84]
- Fixed the six-context Win32 proxy-shell drag path so SDL and SFML can undock from the same visible `EpochChild` shell the user actually grabs in the parented editor, instead of depending on the nested backend child to start the escape. The proxy host is now promoted as a real standalone top-level shell on undock and normalized back into child mode on redock.
- Corrected the Windows parented harness to drag the visible SDL/SFML proxy shell instead of the nested backend child during multicontext checks, which brings the proof path back in line with the real editor interaction model and restores truthful `MidTopLevel=true` evidence under six-pane load.
- Reworked the Win32 SDL/SFML proxy drag contract around the real elevated host hierarchy: intentional drag now measures from the visible proxy host, the host can undock as a true top-level shell without waiting to cross the parent boundary, and redock still resolves on release back into the parented slot.
- Hardened the LM Studio `/api/v1/chat` path with explicit non-stream requests, longer WinHTTP receive timeouts, stricter HTTP read failure handling, and broader response-content fallbacks/logging so slow larger-model warmups are less likely to collapse into a silent `(empty reply)` in the editor chat.
- Added roadmap/runtime/audit guidance for the next backend-lifecycle target: editor should converge toward a single-context OpenGL path, launcher toward a single-context software path, switching should be explicit, and inactive backends should be torn down instead of left running in the background.
- Restored the launcher's older classic steel palette through a scoped GUI theme path, so the launcher can keep its familiar look without forcing the editor back off the darker neutral tool palette.
- Added launcher/editor theme-split guidance to the README, runtime operations doc, and roadmap, and wrote down the future settings-level theme-selector direction as an independent launcher/editor choice instead of one global toggle.
- Corrected the research-intake wording so the repo no longer treats `Changes/` as the default planning-import home or `botface.html` as Epoch roadmap truth by default; staged research now points toward a neutral `workspace/research/staged/` path and reviewed promotion.
- Parsed the current strategy PDFs directly and folded their concrete guidance into the roadmap and AI memory: 2D now stays framed as a vertical slice through real engine spines, chunked cellular simulation is preferred over ECS-per-cell, OpenGL remains the six-month 2D golden path with software as a correctness oracle, and the local AI loop is written down as a gated seed/teacher/verifier system instead of vague self-coding language.
- Added the first explicit AI iteration-packet path under `workspace/ai/iterations/` and surfaced it in the editor AI workspace, so staged task packets now preserve project/model/capture/evidence context in `iteration.json` and `task.md` before any later promotion step.
- Completed the focused six-pane SDL/SFML proxy-shell fix: detached drags now stay on the visible `EpochChild` shell, preserve capture through the child-to-top-level transition, and continue following the mouse as real top-level windows instead of freezing inside the parent and auto-redocking on release.
- Corrected the parented Win32 harness to start proxy drags from the real dock shell, record whether the detached host actually escaped the parent bounds, and wait more honestly for slower cross-thread/backend transitions before judging the run.
- Revalidated focused SDL and SFML six-pane parent runs against the detached-shell contract, then reran the slower all-backends sweep. Both focused runs and the current full matrix now reach `MidTopLevel=true` and `MidEscapedParentBounds=true` for SDL/SFML, but the harness is still synthetic-message driven and should not outrank live manual smoke forever.

## [v0.83.83]
- Corrected the full-grid Win32 harness contract for Raylib so it now treats the docked pane honestly as a direct child with a hidden parked host, instead of falsely expecting a live proxy child where Raylib does not use one.
- Tightened shared button text placement so left-edge buttons stop centering their labels right against the clip edge, which reduces the missing-leading-text seam on the first buttons in shared rows.
- Added more concrete project-shell proof in the editor docks by surfacing `project.paths.txt` and file-exists checks for the manifest, entry source, build script, output, build log, and active script source directly in the Project/Scripts workspaces.
- Tightened the parented SDL and SFML startup path so the docked child window now pushes its slot size back through the backend window object before the first present, instead of waiting for a later resize event to settle the first visible frame.
- Corrected the Win32 parented harness dock-state probe so it reports both truthful dock contracts: direct child panes parented into the grid with hidden helper hosts, and true proxy-host detached panes. Focused SDL and SFML dock -> undock -> redock checks are green again without faking wrapper visibility.
- Removed more width-mutating shared editor labels from the top-bar and segmented button paths so active menu/workspace buttons stop changing their own width just to indicate selection.
- Removed the outstanding MSVC `C5202` mixed-module warning from `Engine/src/runtime.cpp` by keeping the global module fragment limited to preprocessor directives and moving the legacy runtime forward declaration into normal module scope.
- Made generated editor project shells more honest and easier to follow: new shells now root under repo-level `Projects/`, emit broader embedded-engine folder surfaces (`include`, `modules`, `source`, `scripts`, `resource`, `assets`), and write a `project.paths.txt` manifest so the editor/build logs point at concrete files instead of vague output.
- Added stronger project-shell transparency in the editor log by reporting the generated root, manifest, entry source, build script, and default script paths when a game or tool shell is created.
- Fixed the shared preview camera bookkeeping so per-context preview state stops collapsing onto one camera key, and tightened the ground-hit marker fallback order around real editor focus / center-ray behavior.
- Shifted the Windows parent host further toward the intended dark shell and calmer editor palette, while documenting that SDL/SFML proxy-host redock visibility is still an active multicontext follow-up instead of falsely signing it off green.
- Merged the temporary AI-expanded roadmap notes into the real roadmap steering surface and kept the helper-first, evidence-backed automation loop written down for future passes.

## [v0.83.82]
- Turned the generated project shell further into a real child-build surface: the Project workspace now exposes entry source, generated project file, build script, build log, expected Debug output, and an explicit `Build Active Project` action.
- Tightened generated embedded-engine project wording and metadata around the real engine surface instead of only `Engine/include/`: headers, modules, source, scripting, and resources are now treated as part of the honest child-project path.
- Hardened project-local script compilation by adding broader project and repo engine include-root discovery, so generated project scripts no longer rely on one fragile include assumption.
- Fixed a parented Win32 grid follow-up seam by requesting relayout after pane removal, which helps the first-slot collapse case stop leaving stale gaps after a docked window disappears.
- Refreshed the README current snapshot, tightened the public engine pitch, and regenerated the smaller Windows backend proof images from the latest fullscreen six-context source proof instead of leaving those README crops on stale older images.
- Revalidated the modified source with `StaticLib1 | Debug | x64` and `ConsoleApplication1 | Release | x64` before continuing the next project-shell/game-making pass.

## [v0.83.81]
- Turned generated project shells into live editor/runtime data instead of leaving them as write-only files: the editor now discovers non-template `Projects/**/project.epoch.json` manifests at runtime and surfaces them through the existing project-profile path.
- Tightened the game/tool creation loop so a newly created project shell becomes the active editor project immediately, with its runtime target and seed scene state loaded instead of stopping at file generation.
- Made the scripting/project dock more honest for generated projects by resolving project-local `scripts/*.ascript.cpp` sources before falling back to template/engine paths, and by running scripts from explicit resolved source paths rather than assuming everything lives under `Engine/src/scripts`.
- Rebuilt `ConsoleApplication1` in `Debug|x64` and `Release|x64` and rechecked a bounded asset-bearing Debug launch through the existing parented harness after the project-shell/script changes landed.

## [v0.83.80]
- Bumped the public source/version state again so the repo finally advances with the screenshot/runtime pass instead of leaving the source behind the latest multicontext work.
- Moved the Windows multicontext proof path further into the engine itself: smoke capture now forces parented capture runs onto fullscreen-sized window defaults, promotes the parent window to the foreground/topmost path before capture, and grabs the full parent window rectangle instead of relying on harness desktop shots.
- Reset the README proof away from the incorrect launcher-facing image so the next promoted multicontext screenshot can honestly target the fullscreen editor shell instead of a smaller non-editor fallback.
- Fixed the fullscreen parented editor proof so the middle OpenGL slot no longer collapses into the stale square-sized viewport during capture; the promoted README image now comes from the engine-owned fullscreen capture outputs for `v0.83.80`.

## [v0.83.79]
- Bumped the public source/version state again so the next packaged release comes from a freshly versioned repo commit instead of reusing the previously published `v0.83.78` source tag.
- Refreshed the README multicontext proof contract around a fullscreen six-context harness image so the release-facing editor screenshot matches the current source/version line and visibly carries all six backends.
- Kept the Windows and Linux packaged assets tied to the same bumped source commit instead of letting release publication drift ahead of the repo snapshot.

## [v0.83.78]
- Fixed the Windows Release build discovery path for nested `Engine/vcpkg_installed/<triplet>/<triplet>/...` layouts by teaching both [ConsoleApplication1.vcxproj](/C:/Users/iammi/.codex/worktrees/2a8f/epoch_vibed/Engine/examples/ConsoleApplication1/ConsoleApplication1.vcxproj) and [StaticLib1.vcxproj](/C:/Users/iammi/.codex/worktrees/2a8f/epoch_vibed/Engine/examples/StaticLib1/StaticLib1.vcxproj) to resolve the real include/lib root before compiling.
- Recovered the honest visible-host proxy contract for Windows SDL/SFML panes in the parented editor: the backend child now stays parented to its `EpochChild` shell while docked and undocked, proxy host moves are routed back onto the host-owning thread, and the Win32 harness now validates proxy host/child ownership instead of pretending SDL/SFML behave like raw child panes.
- Folded in the latest launcher and platform cleanup: the launcher drops the old Sandbox/Platformer buttons in favor of `2D Studio`, `Open Editor`, settings, update, and quit; Linux gets an explicit `RequestActiveParentLayout()` seam; caret blink returns to a real blink rhythm; and Vulkan copy-buffer logging stops spamming release runs.
- Revalidated the parented six-context editor honestly from both `x64/Debug` and `x64/Release`: full-grid harness runs survived maximize, Raylib/SDL/SFML undock-redock cycles, and early `SFML_Window` close without killing the parent editor.
- Hardened the release packaging contract for the next updater-shell asset: the Windows zip now carries the VC143 CRT app-local instead of depending on a machine-global redistributable, the no-args launcher shell gets a direct smoke pass before publishing, and the Linux/WSL2 asset must be rebuilt from the same bumped source commit instead of drifting from the public tag.

## [v0.83.76]
- Fixed the Linux/WSL2 updater-shell build by importing `context.window` in [aengine.cpp](/C:/Users/iammi/.codex/worktrees/2a8f/epoch_vibed/Engine/src/aengine.cpp), which keeps the tagged source snapshot and the packaged Linux asset aligned to the same working commit.
- Follow-up release packaging now targets matching Windows and Linux updater-shell assets from the same corrected source tag instead of shipping a Linux binary from a different commit than the public source snapshot.

## [v0.83.75]
- Tightened the Win32 parented multicontext redock path so proxy `EpochChild` hosts are reattached and hidden after redock instead of lingering as stray top-level wrapper windows beside the real `GLFW30`, `SDL_app`, or `SFML_Window` panes.
- Hardened the Win32 GUI input bridge around actual focus ownership by tracking `WM_SETFOCUS`/`WM_KILLFOCUS` and rejecting keyboard forwarding when the live Win32 focus is on a different pane, which reduces the editor's cross-pane text/key leakage risk.
- Made the multicontext harness more honest by recording proxy-host state after each drag cycle, so a pass cannot claim green while a helper host is still floating outside the parent.
- Updated the AI request path to tolerate first-detected LM Studio models that reject reasoning configuration by retrying without the `reasoning` field instead of surfacing empty replies.
- Refreshed the roadmap and build/runtime/smoke/AI docs around the current parented-pane contract and left typed-text editor smoke explicitly open as the next missing regression gate.

## [v0.83.74]
- Restored the stable top-row child-surface contract in the Windows parented multicontext editor: `GLFW30`, `SDL_app`, and `SFML_Window` are back to being the visible docked panes, while the helper `EpochChild` wrappers stay hidden instead of owning the slot.
- Fixed the SDL/SFML dock/redock regression by making the real backend child windows dockable again, preserving focus/input on those child surfaces, and keeping the helper hosts out of the visible layout.
- Added the first repeatable Win32 parented harness for single-backend and full-grid validation, then revalidated maximize, drag-undock-redock for Raylib/SDL/SFML, and early visible `SFML_Window` close without killing the parent editor.
- Promoted a new honest six-context Windows proof image from the real `x64/Debug` harness run and updated the README/build/runtime/smoke docs around the actual top-row ownership contract.

## [v0.83.73]
- Normalized the Vulkan editor scene preview onto the same shared `render.preview_grid` camera math already used by OpenGL, so the two backends stop drifting in editor view even when they render the same grid and marker data.
- Kept the Vulkan editor preview on the shared preview camera near/far/fov contract instead of maintaining a separate GLM-only preview path, which reduces cross-backend framing mismatch in the multicontext editor.
- Updated the README and build memory so the scene-preview parity rule is explicit: OpenGL and Vulkan editor previews should stay on the same shared camera/projection source when preview mode is active.

## [v0.83.72]
- Recovered the stable hosted-child top-row multicontext model: SDL and SFML now keep the visible `EpochChild` host as the dock/grid slot owner while the real `SDL_app` and `SFML_Window` children render inside it again, instead of trying to promote those backend children into direct pane ownership.
- Kept the parented grid honest under maximize while preserving the current Raylib path: the visible `GLFW30` pane stays on the backend input bridge, SDL/SFML stop blanking the top row, and the six-context maximized editor proof is stable again from the real asset-bearing `x64/Debug` runtime.
- Revalidated the current hosted layout with real Windows smoke: `Debug|x64` and `Release|x64` both rebuilt cleanly, a maximized six-context run held all six panes visibly, and an early visible `SFML_Window` close still did not kill the parent editor.
- Updated the README proof plus the build/runtime/smoke docs so the multicontext memory now explicitly records the current slot-ownership rule instead of incorrectly assuming every helper host should be hidden.

## [v0.83.71]
- Flattened the launcher into one direct-entry project/tool surface: visible launcher flow now centers on project opening, clean editor launch, contexts/settings, updates, and quit, without the old `Projects/Games/Tools` panel stack or puzzle/game-facing shell.
- Tightened the Win32 parented multicontext grid so layout now operates on the HWND that actually owns the dock slot at that moment, which makes SDL/SFML child takeover transitions more honest during resize/maximize passes instead of skipping panes when the backend child is alive but not yet the parented slot owner.
- Stabilized the shared preview marker by reusing the last honest grid-plane hit before falling back to editor focus, reducing the old camera-follow drift when the center ray goes temporarily parallel to the grid.
- Fixed the docked Raylib child resize path so a maximized parented grid no longer reintroduces top-level chrome-sized growth and push the visible `GLFW30` pane past its slot.
- Revalidated the real asset-bearing Windows path: `Debug|x64` and `Release|x64` both rebuilt cleanly, a maximized six-context editor run held the visible `GLFW30`, `SDL_app`, `SFML_Window`, `Vulkan`, `OpenGL`, and `Software` panes with hidden wrapper hosts, and an early visible `SFML_Window` close no longer killed the parent editor.
- Updated the README proof, roadmap, runtime/build/smoke docs, and AI build memory to reflect the flattened launcher scope, the parent-aware dock-slot rule, the docked-child resize contract, and the helper-first LM Studio workflow using `/v1/responses` plus `reasoning.effort = none` for direct drafting.

## [v0.83.70]
- Corrected the shared preview look marker so it prefers the real center camera ray against the grid plane before falling back to projected editor focus, which makes the visible target spot more honest across the active backends.
- Extended the two-role AI curation path with an MCP snapshot promotion helper and editor action, so staged scene-guidance captures can now move into `Engine/ai/datasets/curated` instead of stopping at local workspace staging.
- Tightened the generated project shell language and launcher-facing project wording so embedded-engine/bootstrap flows read like live project work instead of placeholders or demo-era leftovers.
- Updated the roadmap, README, runtime/build/smoke docs, and AI build memory to reflect the native LM Studio helper endpoint shape, the now-explicit dual generated-project modes, and the continuing marker/child-surface cleanup work.

## [v0.83.69]
- Pushed the generated project shell further into the real embedded-engine path by emitting an `epoch.project.cmake` fragment, documenting `Engine/include/` as the public include root, and teaching generated script stubs to fall back cleanly between the normalized and legacy script API include paths.
- Deepened the shared time spine in the Systems workspace by surfacing the live frame step budget and the max-steps-per-frame clamp beside the existing fixed-step, accumulator, simulated-time, and time-scale diagnostics.
- Tightened SDL/SFML window ownership to prefer the real backend child surface in context/window tracking, then forced hidden helper-host cleanup during dock layout so parented multicontext panes stay closer to one honest visible surface each.
- Lowered the shared editor look marker onto the grid plane more honestly by removing the old elevated default focus bias and shrinking the marker hover height toward the floor.
- Updated README, roadmap, runtime docs, build docs, smoke docs, and AI build memory so the steering surface now reflects the embedded-engine include path, helper `reasoning_content` fallback, deeper time pacing diagnostics, and the continuing multicontext child-surface cleanup.

## [v0.83.68]
- Reworked the Windows SDL/SFML parented multicontext ownership path so the real backend child surface is the visible pane target, while the helper host stays hidden implementation detail instead of surfacing as a fake dock wrapper.
- Corrected the shared preview look marker so editor mode now anchors the marker to the true grid-plane focus spot instead of letting it drift above the floor or follow the camera awkwardly through an elevated orbit target.
- Replaced the stale launcher-side Puzzle Lab path with a real 2D Studio project profile and routed the generic editor-open launcher action into the honest ProjectLauncher shell instead of a vague default editor jump.
- Promoted the roadmap into GitHub-ready markdown at Changes/roadmap.md, kept Changes/roadmap.txt as a compatibility breadcrumb, and folded in the current steering priorities: multicontext child-window truth, include-surface static compilation, the six-month 2D priority track, helper-first local LLM drafting, naming cleanup, and the procedural/time-node future phase.
- Tightened the runtime/build/smoke docs around the same truths: phase-start helper availability checks, real backend child-window ownership, honest multicontext screenshot rules, and the markdown roadmap as the canonical steering surface.

## [v0.83.67]
- Removed the fake shared preview wireframe-box clutter from the common preview mesh so the camera look marker is no longer visually fighting placeholder geometry across the non-Vulkan backends.
- Corrected the SDL/SFML multicontext ownership path around the Win32 multiplexer host/container model instead of treating the backend child HWND as the dock pane itself, and moved those child HWNDs onto the dedicated backend input bridge instead of the full dockable wrapper.
- Added the first dynamic Vulkan preview-marker path by rebuilding the tiny preview mesh when the shared preview camera revision changes, so Vulkan can follow the same live look-hit indicator concept as the other editor preview backends.
- Stopped double-initializing the Software backend inside the Win32 multiplexer and made the parented multicontext host auto-fit the desktop work area, which restored an honest six-context Windows editor proof on normal displays.
- Updated the roadmap, README, runtime docs, and build docs so the project/scripting phase now explicitly supports both duplicated engine-source projects and static engine compilation through `Engine/include/`.
- Added naming convergence to the steering docs as explicit debt: legacy `aengine*`/orphan naming and misleading labels such as `multiplexer` are now part of the planned professional cleanup instead of being treated as harmless background drift.

## [v0.83.65]
- Turned the Systems workspace into a more honest Phase 3 tooling surface by rendering wider engine-generated frame/task graph textures in the dock, adding reusable inline pan/zoom controls, surfacing a first support-tier diagnostic strip for Baseline, Standard, and Extended runtime targets, and hiding SDL/SFML placeholder dock hosts until their real backend child is ready.
- Tightened the shared GUI layer so dock content uses a real inner content region with stronger padding, narrower available-width calculations, and text/image visibility checks that stop wrapped labels from bleeding into neighboring panes such as the Perspective viewport.
- Kept the hardware guidance visible in-editor and in docs: the default compatibility target remains 6-core / GTX 1660 Ti-era desktops plus modern Linux laptops, while heavier renderer/lib support stays a project-level opt-in.
- Strengthened the smoke/docs loop around local helper usage and proof discipline: use the first detected LM Studio model for runtime parity, lean on local helpers for bounded drafts, launch only from asset-bearing output folders, and treat black software captures as invalid proof rather than a successful screenshot.

## [v0.83.64]
- Turned the editor shell further into a real project-driven runtime surface: project play now stays project-only from the editor path, and the Project dock exposes project kind, root, manifest, template family, runtime target, and generated shell status instead of a thin fake launcher strip.
- Added the first generated project shell flow for both game and software/tool projects in `aeditor.scene.cpp`, writing a real project root, manifest, world file, script stub, and README under `Projects/`.
- Expanded the Scripts dock so it now shows source, build action, run action, and honest validation/load diagnostics instead of only a thin run button.
- Replaced the old `PuzzleLab` profile with a launcher-oriented `ProjectLauncher` profile so the editor/project shell stays centered on contexts, settings, project flow, and future automation instead of a stray puzzle testbed.
- Updated the roadmap and build/runtime/smoke/AI docs so the automation loop now explicitly reflects generated project shells, project-only editor play, bounded helper-model drafting, and the first-detected LM Studio model rule.

## [v0.83.63]
- Rewrote the roadmap around the real current direction: project-driven runtime, modular editor workspaces, frame/task graph tooling in the Systems surface, broad automatic hardware support, and explicit support tiers for heavier optional backend/libs.
- Corrected the AI docs from the mistaken three-role wording to the intended two-role engine model: internal EpochBot plus the local MCP/control layer, while local LM Studio models remain development helpers outside the engine runtime role count.
- Added the explicit local MCP manifest and tightened the AI content/docs around raw capture promotion, staged JSONL policy, and deleting outdated training artifacts when the training direction changes.
- Continued the live Systems workspace implementation with engine-generated render/task graph surfaces and reusable GUI integration, then re-verified the Windows Debug and Release example builds.
- Refreshed the README multicontext proof at `v0.83.63` from the real 4K asset-bearing `x64/Debug` editor run after the Vulkan color/layout corrections.

## [v0.83.62]
- Moved the editor workspace strip onto reusable GUI controls so the project/runtime shell is less hardcoded in `aeditor.cpp` and more aligned with the modular GUI direction.
- Refocused the local AI system prompt so the LM Studio oracle teaches EpochBot inside the active project/editor context instead of behaving like a detached generic chat reply.
- Corrected the AI data policy: JSON/JSONL training data stays Git-safe, `workspace/auto_train.jsonl` is now documented as a staging capture path, and only compiled checkpoints/models/caches remain local-only.
- Refreshed README, runtime, smoke, and build docs around the modular workspace shell and the validated local oracle baseline `qwen/qwen3.5-9b`.

## [v0.83.61]
- Finished the first honest project-runtime seam by moving project profiles, script profiles, runtime scene ids, and seed entities under `aeditor.scene.cpp`, while the live editor shell in `aeditor.cpp` now drives `Project`, `Scripts`, `AI`, `Systems`, and `Output` workspaces on top of that data.
- Added the first repo-safe `Engine/ai/` content layout for curated datasets, schemas, evals, manifests, tokenizer metadata, and prompt templates, while tightening the local-only policy for compiled checkpoint/model paths under `workspace/ai/`.
- Expanded `epoch.ai` into the first explicit three-role AI surface: embedded tiny-model planning types, MCP operation capture types, and the active LM Studio teacher/oracle bridge with visible provider/model metadata.
- Documented the working AI/build/smoke method in `Engine/docs/ai_build_memory.md`, refreshed README/runtime/build/smoke docs for the new policy, and collapsed the old loose `release_notes_v*.md` files behind the existing collated history surfaces.

## [v0.83.60]
- Tightened SDL and SFML docking by stripping leftover top-level extended window styles after `SetParent`, which keeps their docked panes from behaving like stray promoted child windows.
- Collated the loose `release_notes_v*.md` files into `Changes/release_notes_version_series_collated.md` and collated the post-`v0.83.0` live work log into `Changes/post_v0_83_work_collated.md`.
- Documented the current commit/test/capture method in `Engine/docs/smoke_capture_automation.md`, refreshed the build/runtime docs around that workflow, and corrected the public wording so the GUI stack is described as a custom automated texture/atlas system.

## [v0.83.59]
- Restored SDL and SFML dock takeover ownership so the real backend child window remains the active pane after docking settles, while the placeholder host stays available only as the container.
- Unified Vulkan clear color with the shared context palette so Vulkan no longer drifts visually from the other editor backends.
- Pulled the uploaded `botface.html` into repo history, then restored the cleaner UTF-8 working copy on top so the worktree and GitHub stay aligned without preserving the broken encoded variant.

## [v0.83.53]
- Added editor viewport drag-pan so held left mouse movement now slides the preview focus while wheel zoom and right-drag orbit remain active.
- Added a shared camera look-hit marker derived from the center view ray and projected it through the active preview backends so the current focus spot is visible in-editor.
- Tightened software preview invalidation and telemetry churn by tracking camera revisions directly and only re-emitting software framebuffer gauges when their values actually change.
- Captured a fresh 4K six-context editor proof from the asset-bearing local runtime for the README.

## [v0.83.52]
- Aligned the Windows resource move into `Engine/resource/` at the repo/build level, including the CMake resource path and the active Visual Studio shared-item surface.
- Refreshed the editor chrome toward a proper tool-style shell with `File`, `Edit`, `Asset`, `Window`, `Tools`, and `Help` plus mode/system tabs instead of fake project placeholders.
- Added mouse-wheel zoom for the editor preview camera and surfaced the live zoom value in the inspector/output panels.
- Captured and documented the refreshed multicontext editor run from the asset-bearing `x64/Debug` runtime so the README reflects the actual current local launch path.

## [v0.83.51]
- Kept the Windows multi-context host visible for docked SDL and SFML panes so backend child windows no longer disappear behind the editor host during local smoke runs.
- Improved Windows host/child handle matching in the multiplexer so resize, enqueue, cleanup, and dock layout logic follow the real live pane handle instead of only the original host HWND.
- Documented the asset-bearing output-folder launch path for multi-context smoke tests so local editor runs match the runtime asset layout used by the docked backends.

## [v0.83.4]
- Rolled `main` forward after the packaged `0.83.3` updater-shell release so the public binary can demonstrate the live fallback path into the newer source revision with the repaired runtime-version comparison.
- Fixed managed-vcpkg baseline rewriting inside updater sandboxes so the source fallback no longer walks up to the Epoch repo's own `HEAD` when it needs a vcpkg registry git revision.
- Made the source-update worker ignore any inherited `VCPKG_ROOT` and pin a worker-local managed toolchain instead, so updater rebuilds stay isolated from preexisting user vcpkg installs.

## [v0.83.3]
- Rolled `main` forward after the packaged `0.83.2` updater-shell hotfix release so the public binary can demonstrate the full release-to-source update path against a newer source revision.
- Trimmed the source-update manifest down to the graphics-only `SFML` and non-audio `raylib` feature set so updater-managed rebuilds no longer depend on the flaky `libogg`/`vorbis`/`openal` download chain.

## [v0.83.2]
- Fixed the updater worker's managed-vcpkg Git `HEAD` detection so the public shell can continue past managed tool bootstrap and reach the real restore/build/restart path during source fallback updates.

## [v0.83.1]
- Fixed the updater-shell source fallback so it always uses the updater-managed `vcpkg` toolchain instead of mutating any configured local `VCPKG_ROOT` checkout during the rebuild path.

## [v0.83.0]
- Shipped the first dedicated `Release|x64` updater-shell runtime as the downloadable `main.zip` payload, so first launch now lands directly on the minimal bootstrap UI instead of the full launcher surface.
- Replaced the source-updater dependency bootstrap with a managed toolchain flow that can download `vcpkg`, provision Git when needed, patch the `glad` port for current CMake, and rebuild from source without assuming a preinstalled development environment.
- Moved live source-update work into shorter managed work roots and kept the console/process output readable, which makes the shell demonstration path much easier to follow while leaving the rebuilt runtime path aimed at the full engine.

## [v0.82.60]
- Shipped the updater shell as a dedicated software-only bootstrap runtime with a single large update action and a minimal packaged payload.
- Changed updater-shell updates to close the shell window before the console-driven update work begins, preventing the shell from hanging inside the render loop during package or source updates.
- Verified the live packaged shell can fall through from release `0.82.60` to newer source builds on `main`, rebuild the runtime, replace the binaries, and relaunch successfully.

## [v0.82.51]
- Rolled the active repo surfaces forward after the packaged `0.82.50` updater release so `main` stays ahead of the downloadable runtime again.

## [v0.82.50]
- Shipped the Windows source updater handoff fix so the detached worker resolves the live executable path correctly, replaces the rebuilt runtime from `<source-root>/x64/Debug`, and relaunches the updated app cleanly.
- Moved native `vcpkg` and `MSBuild` output into updater log files so restore/build activity no longer dumps raw junk into the live console.
- Switched the sample project include/library paths to repo-relative vcpkg locations so source snapshot builds no longer depend on hardcoded local directories.

## [v0.82.49]
- Rolled the active repo surfaces forward after the packaged `0.82.48` detached-worker updater release so `main` stays ahead of the downloadable runtime again.

## [v0.82.48]
- Moved Windows source updates into a detached worker process so download, restore, build, replacement, and relaunch can continue after the live runtime exits.
- Kept source updates pointed at the direct `main.zip` repository snapshot, added a visible-by-default worker path, and allowed silent worker launches with `EPOCH_UPDATER_SILENT=1`.
- Raised the worker-side build tolerance to three MSBuild attempts after restore and extended the runtime unlock wait so the final replacement step is less timing-sensitive.

## [v0.82.47]
- Rolled the active repo surfaces forward after the packaged `0.82.46` updater repair release so `main` stays ahead of the downloadable runtime again.

## [v0.82.46]
- Kept the source updater on the direct `main.zip` repository snapshot instead of GitHub release assets.
- Adjusted the source rebuild path so it restores manifest dependencies first, then retries the compile pass once before giving up.
- Kept `epoch_source_update.log` and `epoch_update_handoff.log` as the runtime-side diagnostics for rebuild and replacement failures.

## [v0.82.45]
- Rolled the active repo surfaces forward after the packaged `0.82.44` updater repair release so `main` stays ahead of the downloadable runtime again.

## [v0.82.44]
- Replaced the Windows updater `system()/cmd/start` path with hidden process launches for `vcpkg`, `MSBuild`, and the final handoff batches.
- Added `epoch_source_update.log` alongside `epoch_update_handoff.log` so source rebuild failures and post-build replacement failures leave concrete runtime-side diagnostics.
- Fixed the updater source-version path to point at `Engine/modules/aengine.version.ixx` instead of the stale `version.txt` location.

## [v0.82.43]
- Rolled the active repo surfaces forward after the packaged `0.82.42` release so `main` stays ahead of the downloadable runtime again.

## [v0.82.42]
- Repaired the Windows updater handoff batch again by switching to proper batch variable quoting and explicit executable replacement.
- Added `epoch_update_handoff.log` in the runtime folder so failed packaged/source handoffs leave behind a concrete trail instead of silently stalling.
- Refreshed the active release surfaces for the repaired `0.82.42` runtime drop.

## [v0.82.41]
- Repaired the Windows updater handoff so packaged and source updates explicitly replace the runtime executable instead of silently leaving the old binary in place.
- Kept the updater logging path sanitized so updater status stays readable in the commandline pane while the handoff runs.
- Refreshed the active release surfaces for the repaired `0.82.41` runtime drop.

## [v0.82.40]
- Routed updater status back through Epoch logging with sanitized updater messages so the commandline pane stops showing the raw `♪◙` newline artifacts.
- Kept the newer-source fallback intact so a confirmed update still rebuilds from source when no newer packaged runtime exists.
- Refreshed the active release surfaces for the repaired updater runtime drop.

## [v0.82.39]
- Rolled the active repo surfaces forward after the packaged `0.82.38` release so `main` stays ahead of the downloadable runtime again.

## [v0.82.38]
- Fixed updater fallback so a confirmed update now rebuilds from source when `main` is newer and no newer packaged runtime exists.
- Removed the duplicate source-version probe so source update checks stop printing the same source comparison twice.
- Simplified the updater status path to one direct console stream during source/package update work.

## [v0.82.37]
- Rolled the active repo surfaces forward after the packaged `0.82.36` release so `main` stays ahead of the downloadable runtime again.

## [v0.82.36]
- Restored a single visible updater status path by only emitting direct console output when the engine logger is not already configured to own console output.

## [v0.82.35]
- Rolled the active repo surfaces forward after the packaged `0.82.34` release so `main` stays ahead of the downloadable runtime again.

## [v0.82.34]
- Removed the updater module's extra direct console writer so update status now appears once through the engine logging path instead of duplicating in the commandline view.

## [v0.82.33]
- Rolled the active repo surfaces forward after the packaged `0.82.32` release so `main` stays ahead of the downloadable runtime again.

## [v0.82.32]
- Reworked updater version probes to download into unique temp files instead of a shared `remote_version.txt`, preventing file-in-use failures when packaged and source checks run back to back.
- Kept the source-update availability messaging separate from packaged-release detection so the updater can point at `main` cleanly without confusing the release path.
- Cleaned the editor update confirmation modal spacing and trimmed the README snapshot back to a concise current-state summary.

## [v0.82.31]
- Rolled the active version and docs forward to `v0.82.31` immediately after the `0.82.30` updater rebuild release so the live tree stays ahead of the packaged runtime again.

## [v0.82.30]
- Restored the source-update flow so it now downloads the latest `main` snapshot, restores manifest dependencies with `vcpkg`, rebuilds `ConsoleApplication1`, and replaces the running runtime from the rebuilt output instead of stopping after extraction.
- Fixed updater version comparison so builds newer than the latest packaged release stop trying to downgrade themselves.
- Normalized updater console line emission for redirected output so captured update logs stop leaking raw CR/LF control glyphs.

## [v0.82.29]
- Rolled the active version and docs forward to `v0.82.29` immediately after the `0.82.28` update-flow release so the live tree stays ahead of the packaged runtime again.

## [v0.82.28]
- Cleaned up the editor update confirmation flow so packaged updates and source downloads are separate explicit actions instead of one ambiguous destructive button.
- Added a source-snapshot command that downloads the latest `main` source tree beside the runtime without rebuilding or replacing the running binary.

## [v0.82.27]
- Rolled the active version and docs forward to `v0.82.27` immediately after the `0.82.26` release so the live tree stays ahead of the packaged updater build.

## [v0.82.26]
- Restored visible console feedback for GUI-triggered update checks so the updater prints what it is doing and explicitly reports when no newer packaged release is available.

## [v0.82.25]
- Rolled the active version and docs forward to `v0.82.25` immediately after the fixed `0.82.24` full release so the live tree stays ahead of the packaged updater snapshot.

## [v0.82.24]
- Fixed the updater to query the latest real GitHub release instead of reading `main`, so the reported version and the downloadable `main.zip` asset stay in sync.
- Added GitHub API headers to the download helper so updater version checks use the release API cleanly.

## [v0.82.23]
- Rolled the active version and docs forward to `v0.82.23` immediately after the fixed `0.82.22` release tag so the updater has a new target to detect.

## [v0.82.22]
- Fixed the updater to target the shipped `main.zip` release package instead of stale direct-executable URLs, and aligned the built-in version URL to `Engine/modules/aengine.version.ixx`.
- Updated the Windows install path so the updater expands the package in place and replaces the runtime directory, rather than pretending GitHub serves a single replacement `.exe`.
- Routed updater status lines through Epoch logging so version/download messages no longer leak raw control-character glyphs into captured command output.

## [v0.82.21]
- Rolled the active version and current docs forward to `v0.82.21` immediately after the `0.82.20` release tag so the updater has a newer target to detect.

## [v0.82.20]
- Rolled the active version, README, docs, and release-note surfaces forward to `v0.82.20` so the updater has a fresh live target to detect during manual testing.

## [v0.82.19]
- Fixed the self-update flow so it now targets the currently running executable instead of the old hardcoded `updater.exe` replacement path.
- Added explicit failure reporting for update handoff/install failures so the app no longer looks like it updated successfully when the binary download or replacement step did not complete.
- Refreshed the active version surfaces to `v0.82.19`.

## [v0.82.18]
- Fixed the remaining Windows script compiler spawn bug by using a direct executable launch for real LLVM paths instead of the PATH-search variant, which was still splitting `Program Files` incorrectly during editor-run script builds.
- Refreshed the active version surfaces to `v0.82.18`.

## [v0.82.17]
- Consolidated the older versioned release-note markdowns into a single archive file so the `Changes/` folder keeps one historical notes surface while new releases continue to get their own markdown.
- Removed the accidentally tracked Windows `.exp` script-build artifact from the repo and added ignore rules for `.exp` files and the local `Temp Script Test/` workspace.
- Refreshed the active version surfaces to `v0.82.17`.

## [v0.82.16]
- Fixed the Windows editor script compiler path so it launches `clang++` directly instead of routing through a fragile shell command string that broke on `Program Files` paths.
- Kept the script source itself ASCII-clean and re-verified the rebuilt debug app launch from `x64/Debug` after the compiler-path fix.

## [v0.82.15]
- Replaced the old filewatch-oriented editor scripting flow with an engine-owned `Run` path that compiles and executes scripts against a stable host API, and swapped the default editor script to `rotate_all_entities`.
- Added reusable shared preview cameras with `Editor` and `FPS` modes, keyboard movement, and right-mouse look so the multicontext editor scene is no longer stuck on a fixed preview angle.
- Hardened updater version parsing so remote checks can consume either plain semver text or the full `aengine.version.ixx` module without printing BOM/comment garbage, then refreshed the active README/docs/version surfaces to v0.82.15.

## [v0.82.14]
- Rewrote the active README opening and feature map so Epoch is described more accurately as a world-class, modules-first, AI-enabled engine instead of a minimal runtime summary.
- Expanded the public feature list to call out internalized engine bootstrap, entry-point flexibility, multi-context orchestration, cross-platform build/editor freedom, module-first architecture, task-graph scripting reload, telemetry, and updater plumbing.
- Added direct markdown links from the README to the active docs, build surfaces, scripts, and project files, then refreshed the active version metadata to v0.82.14.

## [v0.82.13]
- Aligned the active backend base clear color to the darker Vulkan-style palette so mixed-context launches feel more like one engine instead of six unrelated demos.
- Moved SFML ahead of Vulkan in the default parented dock order and fixed the SFML shared scene preview so its grid/3D view stays clipped to the scene viewport instead of bleeding through GUI space.
- Rebuilt `StaticLib1` and `ConsoleApplication1` in `Debug|x64`, launched the real binary from `x64/Debug`, and verified the live top row now orders Raylib, SDL, and SFML ahead of Vulkan.

## [v0.82.12]
- Split the old mixed editor/menu flow into a launcher-first workflow where projects and games are chosen from the launcher and the editor focuses on desktop-style file, edit, scene, command, and help menus.
- Added scene preview switching in the editor between `Editor` and `None`, and gated the in-app update action behind an explicit confirmation modal because it can replace binaries and restart the session.
- Rebuilt `ConsoleApplication1` in `Debug|x64`, launched the real binary from `x64/Debug`, and refreshed the active README/docs to match the new launcher/editor workflow.

## [v0.82.11]
- Fixed parent-host shutdown ownership so only panes still docked under `EpochParent` close with the host, while undocked SDL, Raylib, and SFML panes survive as independent top-level windows.
- Fixed the last-window shutdown path so the process now exits when the final surviving undocked pane closes, even when that backend window is owned by a render thread.
- Re-verified live undock/redock and parent-close behavior on SDL, Raylib, and SFML, then refreshed the active README and current docs for the current engine tree.

## [v0.82.10]
- Restored real manual dock/undock interaction without the temporary `Alt` requirement by using a dedicated pane drag strip instead of hijacking the whole content surface.
- Re-verified live left-drag undock/redock behavior across SDL, Raylib, SFML, OpenGL, and Software, and confirmed the parent host still closes the full session cleanly afterward.
- Refreshed the active README and current docs to advertise the v0.82.10 manual docking interaction repair.

## [v0.82.9]
- Restored live undock/redock behavior by preserving each pane's original dock parent across future drags instead of forgetting it once a pane becomes top-level.
- Changed the docking grid arranger to lay out only panes that are currently parented under the live `EpochParent`, so undocked windows stop stealing grid slots or snapping back unexpectedly.
- Re-verified SDL, Raylib, SFML, OpenGL, and Software with live undock/redock interaction runs, then confirmed the parent host still closes the whole session cleanly.

## [v0.82.8]
- Fixed the parent-window shutdown path so docked backend panes close in place instead of being undocked during host shutdown.
- Re-verified that the parented multi-context layout stays docked at runtime and the process still exits when the main `EpochParent` host closes.
- Refreshed the active README and current docs to advertise the v0.82.8 docking/shutdown hotfix without rewriting historical release files.

## [v0.82.7]
- Hardened the parent-window close path so all tracked docked contexts receive shutdown and the engine session exits cleanly when the main host closes.
- Restored backend confirmation logging behind configurable `EPOCH_ENABLE_*CONFIRMATION_LOGS` switches so startup, upload, and teardown confirmations can be enabled without reintroducing per-frame spam.
- Refreshed the active README and current docs to advertise the v0.82.7 shutdown and diagnostics pass while keeping historical release files intact.

## [v0.82.6]
- Added the shared editor preview grid to the SDL and Software backends so they present the same scene-view framing as the existing GPU preview paths.
- Stopped the parented Raylib backend from replacing the original WGL device context after initialization, which stabilizes mixed-context startup.
- Refreshed the active README and current docs to advertise the v0.82.6 backend parity/stability pass without rewriting historical release files.

## [v0.82.5]
- Removed the unconditional per-frame Vulkan runtime diagnostic trace from normal runs and left it behind an explicit debug macro.
- Throttled slow-frame renderer warnings so startup no longer floods `Renderer.log` while the backend grid spins up.
- Removed the hot-loop SFML "frame render path" logging that was bloating `SFML.log` during startup.
- Refreshed the README and docs so the active build surfaces now cover Visual Studio, MSBuild, CMake presets, VS Code, shell scripts, and binary-folder launch paths.

## [v0.82.4]
- Restored the Vulkan editor scene pass so the backend renders the grid preview instead of only clearing the scene viewport.
- Matched the Vulkan editor preview framing and palette more closely to the OpenGL editor view, including calmer scene/background behavior during mixed-backend runs.
- Hardened the SFML close path so GPU atlas cleanup happens while the context is still valid and mixed-context shutdown stays quiet.

## [v0.82.3]
- Aligned the Vulkan preview palette with the OpenGL editor palette so the active backends present the same core scene colors.
- Stabilized Vulkan GUI presentation by keeping the last valid GUI batch during producer gaps and matching GUI atlas sampling behavior more closely to OpenGL.
- Refreshed the active release surfaces and notes to advertise the v0.82.3 Vulkan preview update without rewriting historical release files.

## [v0.82.0]
- Rebranded the public documentation and release metadata from the previous public name to Epoch.
- Synchronized engine version helpers and the legacy compatibility version header to v0.82.0.
- Moved the standalone `legacy/` archive into `Engine/legacy/` and added a single in-engine legacy archive guide.
- Rewrote the README, docs index, build guides, configuration guide, engine analysis, context audit, and runtime notes to match the current repository layout and backend status.

## [v0.81.23]
- Bumped the engine version constants to v0.81.23 so runtime banners and tooling remain aligned.
- Refreshed the README snapshot, configuration guide, and engine analysis notes to advertise the v0.81.23 release tag.
- Added the v0.81.23 release notes to keep the published metadata in sync.
- Documented the C++23 module-first baseline so build tooling and packagers can align module scanning and compiler expectations.

## [v0.72.9]
- Bumped the engine version constants and legacy headers to v0.72.9 so runtime banners and tooling remain aligned.
- Refreshed the README snapshot, configuration guide, and engine analysis notes to advertise the v0.72.9 release tag.
- Added the v0.72.9 release notes to keep the published metadata in sync.

## [v0.72.8]
- Converted additional legacy headers into module partitions so atlas utilities (`aatlasmanager.hpp`, `aatlastexture.hpp`) now expose `import atlas.manager;` / `import atlas.texture;`, the CLI surface is mirrored via `import acommandline;`, ECS mirrors (`import aentity.*`) stay in sync, and umbrella aggregates mirror the legacy include surface.
- Synced version metadata and documentation to advertise v0.72.8 with the refreshed import surface.
- Confirmed updater/configuration guidance pulls version strings from the runtime helpers so launch manifests match v0.72.8.

## Older releases
- Historical release notes remain in `Changes/release_notes_v*.md`.
- The older pre-rebrand release history has been preserved, but the public docs now use Epoch branding consistently.

## [v0.83.54]
- Wired Win32 child-window wheel and text/key messages back into the editor GUI path so docked editor contexts can finally deliver wheel zoom and live AI chat input instead of relying on dead plumbing.
- Flattened the preview look-hit marker onto the grid plane and removed the floating camera-to-hit indicator so the center-look spot reads like an editor targeting aid instead of a hovering false collision.
- Replaced the placeholder asset/actions shell with real editor entity controls for adding meshes, lights, spawns, duplicating the current selection, deleting the current selection, and sending a live scene prompt into the AI panel.
## [v0.83.66]
- Rewrote the roadmap around the approved phase order: project/runtime shell, Systems renderer tooling, a new dedicated time-system spine, the two-role AI training spine, UI/editor maturity, later procedural world and time-node authoring, and the automation/capture loop.
- Added the first shared simulation-clock ownership to `core.time`, including fixed-step accumulation, pause/resume, time scaling, single-step support, and editor-facing stats that can travel through the engine instead of living as per-backend guesswork.
- Surfaced the new time-spine state in the editor Systems workspace beside the existing render/task graph surfaces, widened the generated graph textures for broader tooling coverage, and kept the compatibility-tier guidance visible alongside the new pacing/time diagnostics.
- Continued the project/runtime cleanup by removing one more hardcoded sample-launch branch from the older editor loop and keeping the project-driven play path centered on real scene ids.
- Updated README, runtime/build/smoke/AI docs around the time-based engine direction, the GPU-driven baseline renderer plan, the later SpeedTree-like procedural/node authoring phase with future O2L integration, and the helper-first workflow that can use up to four parallel prompts per loaded local helper model.

## [Unreleased]
- Removed the duplicate Win32 multicontext bring-up path so context layout and render-thread startup only happen once during engine/editor launch instead of being re-fired from both `Initialize()` and the outer startup loop.
- Added a short per-context startup stagger on both Win32 and Linux multicontext managers so heavy backends do not all initialize on the same instant, which reduces random bring-up stalls and gives the editor a calmer startup path.
- Stopped OpenGL from swapping an unready first frame while its quad pipeline is still unavailable, reducing the intermittent load-time flicker/blank-present seam during startup.
- Switched Win32 SDL/SFML proxy drags to child-relative screen math and kept proxy panes detached until mouse release, so parented top-row drags stop snap-redocking mid-drag and stop picking up host-frame drift.
- Extended the parented Win32 harness to record the proxy release-redock contract explicitly (`ContractMode`, `LateDetached`, `EndRedocked`) instead of pretending proxy-child panes should redock live like direct-child panes.
- Revalidated focused serial SDL and SFML parented drag cycles against the new contract and kept the close/destroy multiplexer logging visible for user-close vs orderly shutdown diagnosis.
- Added small shared GUI clip slack to reduce first-glyph clipping on left-edge buttons and text rows without touching launcher palette choices.
- Tightened the Project and AI workspaces with clearer play/build readiness rows, exact generated project path logging, and clearer staged-vs-curated AI role/evidence messaging.
- Split the Windows parented multicontext harness into two honest proof modes: normal serial runs keep dock/undock evidence clean, while the new opt-in `-CaptureStartupProof` path records startup image samples for first-frame rendered-content checks without replacing the drag baseline.
- Revalidated focused single-backend SDL and SFML dock -> undock -> redock truth after the harness split, and captured separate startup-proof runs that now report non-empty rendered-content samples for both backends.
- Added a small shared GUI text inset so left-edge buttons stop clipping the first glyph against the content clip boundary.
- Tightened the parented Win32 multicontext harness so drag proofs poll for real undock/redock state transitions instead of assuming a fixed 250 ms sample window, which was under-reporting SFML full-grid escape/return behavior.
- Confirmed with new Debug evidence that SFML full-grid runs can now show real `Posted Undock -> Posted Redock -> Redock begin/end` sequencing in the engine log, while focused SDL/SFML single-backend runs remain green.
- Added more in-editor project proof rows for generated project shells and script sources so the Project and Scripts workspaces expose concrete file/output/log locations rather than only status text.
- Fixed the Win32 SFML redock decision to use the actual mouse-release screen point instead of stale drag history, which was leaving some full-grid runs detached even when the release returned inside the parent window.
- Switched Win32 proxy drag tracking to the real screen cursor for detached SDL/SFML-style child drags so grabbed panes stop drifting or “running away” as their host/child relationship changes during undock.
- Clarified the AI workspace with explicit eval-suite visibility plus staged capture and promotion paths in the editor log so the two-role EpochBot/MCP flow stays reviewable instead of reading like blind autonomy.

